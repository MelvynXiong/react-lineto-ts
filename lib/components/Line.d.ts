/// <reference types="react" />
import { LineProps } from '../interface';
export default function Line({ x0, y0, x1, y1, borderColor, borderStyle, borderWidth, zIndex, className, }: LineProps): JSX.Element;
